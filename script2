#Time zone
ln -sf /usr/share/zoneinfo/Europe/London /etc/localtime

#Set hostname
echo archpc > /etc/hostname

#Install bootctl
bootctl install

#Configure  EFI
echo options root=PARTUUID="&(findmnt -fn -o UUID /dev/sda3)" rw > /boot/loader/entries/arch.conf

rm script*

